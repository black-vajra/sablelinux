# EliteBook BOINC reference procedures

These files preserve the exact successful BOINC implementation validated on
the secondary SableLinux host `vulfen` during July 18–19, 2026.

They are engineering evidence, not canonical release scripts. They may contain
`/home/sable`, `/tmp`, and other EliteBook-specific assumptions. No compiled
binaries, BOINC account keys, GUI RPC secrets, client state, or project
workloads are stored here.

The authoritative Z890 implementation must rebuild from pinned upstream inputs
and convert these reference procedures into normalized repository-owned
scripts.
