# BOINC integration

This directory holds SableLinux BOINC integration work.

`reference/elitebook-20260718` preserves the successful secondary-host
implementation. Canonical work on the Z890 must add pinned source acquisition,
checksum validation, repeatable builds, versioned installation, service setup,
resource policy, smoke tests, logs, manifests, and artifact hashes.
